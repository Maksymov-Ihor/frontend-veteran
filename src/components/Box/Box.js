import styled from "styled-components";
import {
    color,
    space,
    layout,
    flexbox
} from 'styled-system';

const Box = styled('div')(
    color,
    space,
    layout,
    flexbox
);

export default Box;